{
    "query": {
        "sql": "select * from fluent-bit where match_all('nginx')",
        "from": 0,
        "size": 100
    },
    "aggs": {
        "histogram": "select histogram(_timestamp, '1 hour') AS key, count(*) AS num from query GROUP BY key ORDER BY key limit 10"
    }
}